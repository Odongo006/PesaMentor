default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  schema_search_path: public

development:
  <<: *default
  database: pesamentordb
  username: postgres
  password: HenryDB

test:
  <<: *default
  database: pesamentordb
  username: postgres
  password: HenryDB

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
